#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase
 *
 *
 * Return: 0 if successful
 *
 */
int main(void)
{
	char k = 'a';

	while (k <= 'z')
	{
		putchar(k);
		k++;
	}
		putchar('\n');
	return (0);
}
