#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase
 *
 *
 * Return: 0 if successful
 *
 */
int main(void)
{
	char k;

	for (k = 'a'; k <= 'z'; k++)
	{
		putchar(" %c");
	}
	for (k = 'a'; k == 'z'; k++)
	{
		putchar(" \n");
	}
	return (0);
}
