#include "holberton.h"

/**
 *_isupper - check if C character is uppercase.
 *
 *
 *@c: is a letterthat will determine the return.
 *
 * Return: 1 if the letter is uppercase and 0 if lowercase.
 */

int _isupper(int c);

{
int c;

for (c = 'A'; c <= 'Z'; c++)
{
for (c = 'a'; c <= 'z'; c++)
putchar(0);
}
putchar(1);
return (0);
}
